#include <iostream>
using namespace std;

int main() {

    for (int k = 0; k < 500; k++) {
        cout << (k + 1) * 2 << endl;
    }
    
    /* int acumulador = 0;
    while (acumulador < 500) {
        cout << (acumulador + 1) * 2 << endl;
        acumulador++;
    } */

    /* int acumulador = 0;
    do {
        cout << (acumulador + 1) * 2 << endl;
        acumulador++;
    } while (acumulador < 500); */
    
    return 0;
}