#include <iostream>
using namespace std;

int main() {

    for (int k = 1000; k >= 1; k--) {
        cout << k << endl;
    }
    
    /* int acumulador = 1000;
    while (acumulador >= 1) {
        cout << acumulador << endl;
        acumulador--;
    } */

    /* int acumulador = 1000;
    do {
        cout << acumulador << endl;
        acumulador--;
    } while (acumulador >= 1); */
    
    return 0;
}

/*
Bueno, me equivoque jajjaa si se puede modificar el bucle foooorr pero bueno de cada error me llevo algo asi
que sin problemas :) ahora ya se que si se puede modificar los argumentos del bucle para hacer todo mucho
mas practico aunque no se porque fallo la anterior vez.

NOTA INCREIBLE: Yo solito descubri el -- jajaja o sea fue como ingenieria inversa de pensar si es ++
pues -- tambien funciona no? xD
*/